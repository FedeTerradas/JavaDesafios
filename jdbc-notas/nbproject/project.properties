auxiliary.ModulePathsProblems.test.paths.check=false
build.generated.sources.dir=${build.dir}/generated-sources
debug.modulepath=\
    ${run.modulepath}
debug.test.modulepath=\
    ${run.test.modulepath}
excludes=
includes=**
javac.modulepath=
javac.processormodulepath=
javac.test.modulepath=\
    ${javac.modulepath}
run.modulepath=\
    ${javac.modulepath}
run.test.modulepath=\
    ${javac.test.modulepath}
src.dir=src
test.src.dir=test
build.dir=build
build.classes.dir=${build.dir}/classes
source.encoding=UTF-8
javac.release=17

application.title=jdbc-notas
dist.dir=dist
dist.jar=${dist.dir}/${application.title}.jar

lib.dir=lib
javac.classpath=${lib.dir}/mysql-connector-j-9.5.0.jar
run.classpath=${javac.classpath}:${build.classes.dir}

main.class=jdbc.clase.JdbcClase
